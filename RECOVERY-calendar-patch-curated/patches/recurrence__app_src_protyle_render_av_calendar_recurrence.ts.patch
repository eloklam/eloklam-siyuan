diff --git a/app/src/protyle/render/av/calendar/recurrence.ts b/app/src/protyle/render/av/calendar/recurrence.ts
index fa0768941..6d12ceaf5 100644
--- a/app/src/protyle/render/av/calendar/recurrence.ts
+++ b/app/src/protyle/render/av/calendar/recurrence.ts
@@ -77,6 +77,11 @@ export const parseRecurrence = (value: unknown): ICalendarRecurrence | undefined
 
     const str = value.trim().toUpperCase();
 
+    // Human-friendly "None" value is treated as no recurrence.
+    if (str === "NONE") {
+        return undefined;
+    }
+
     // Simple frequency-only strings
     if (isValidFreq(str)) {
         return { freq: str as ICalendarRecurrence["freq"] };